# Frontend environment variables (Vite expects variables prefixed with VITE_)
# Copy this file to `.env` or `.env.local` and edit values as needed.

# Backend base URL (omit trailing slash). Default for local dev is http://localhost:3001
VITE_BACKEND_URL=http://localhost:3001
